# Auth0 Database Connections API POC

Install the dependencies with npm:

```bash
npm install
```

Comment out the last lines of server.js, and follow instructions from the comment above the last lines

```bash
npm run start
```
